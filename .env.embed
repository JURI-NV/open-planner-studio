# Actief alleen tijdens `npm run build:embed` (via `vite build --mode embed`).
# De waarde zelf wordt niet als URL-prefix gebruikt (zie serverBackend.ts's apiBase()) —
# dit is uitsluitend de build-time AAN/UIT-vlag voor de derde fileAccess-backend (T1.1-T1.3).
VITE_JURI_API_BASE_URL=embedded
