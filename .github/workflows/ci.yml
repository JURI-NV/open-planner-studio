name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'ubuntu-22.04'
          - platform: 'windows-latest'
          - platform: 'macos-latest'

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Install Rust targets (macOS universal)
        if: matrix.platform == 'macos-latest'
        run: rustup target add aarch64-apple-darwin x86_64-apple-darwin

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install frontend dependencies
        run: npm install

      - name: Build Tauri app
        run: npm run tauri build -- --no-bundle

  test:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install frontend dependencies
        run: npm install

      - name: Typecheck
        run: npx tsc --noEmit

      # scripts/ en tests/ vallen buiten `include: ["src"]` van de hoofd-tsconfig en draaien via
      # esbuild, dat types STRIPT zonder ze te controleren (bevinding K10b). Zonder deze stap wordt
      # ~19k regels TypeScript — inclusief de CI-blokkerende verify-docs/verify-examples en de hele
      # testharnas — nooit getypecheckt.
      - name: Typecheck scripts/ en tests/
        run: npx tsc --noEmit -p tsconfig.tests.json

      - name: Run planning/CPM regression suite
        run: bash tests/planning/run.sh

      - name: Verify example projects
        run: npm run verify:examples

      - name: Verify docs manifest
        run: npm run verify:docs
