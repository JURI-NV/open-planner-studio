# Alle gebundelde testartefacten die run.sh genereert (esbuild-output per check-*.ts).
# Eén patroon i.p.v. een handmatige lijst, zodat hij niet kan achterlopen (zelfde opzet als tests/library).
.*.mjs
